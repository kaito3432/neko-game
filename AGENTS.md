# AGENTS.md - にゃんチェイス開発ルール

## 最重要
このプロジェクトは既存ゲーム機能がすでに動作している。
新機能追加のために、完成済み機能を無断で変更・簡略化・削除しないこと。

---

# 1. 変更ルール

- ユーザーから依頼されたPhaseだけを実装する。
- 次Phaseの機能を先回りして実装しない。
- 既存コードを全面的に書き換えない。
- 不要なリファクタリングを同時に行わない。
- 修正範囲は可能な限り小さくする。
- 既存関数名・イベント名・通信仕様を理由なく変更しない。
- 既存UIを理由なく変更しない。
- 既存ゲームバランスを理由なく変更しない。

---

# 2. 絶対に勝手に変更しないもの

明示的な指示がない限り以下を変更しない。

- 基本ゲームルール
- 11ターン制
- ネコ移動ルール
- 柴犬警察の移動・探索ルール
- CPU難易度ロジック
- CPU勝率調整値
- 特殊スキルの能力内容
- 既存オンライン部屋対戦
- 既存の勝敗判定
- 既存アニメーション・演出
- 既存BGM / SE
- iPhone向けに完成しているレイアウト

---

# 3. オンライン関連

- 既存オンライン部屋対戦を壊さない。
- ランダムマッチは既存部屋対戦と分離して追加する。
- サーバーが正であるべきデータをクライアントだけで確定しない。
- にゃんコイン、ランク、所持品、課金、オンライン報酬は改ざん可能なクライアント値を信用しない。
- 通信失敗時にゲーム全体が停止しないようにする。
- 二重送信・二重報酬を防止する。
- 同一処理の再送に耐えられる設計を優先する。

---

# 4. クロスプラットフォーム

- 現在はiOS優先。
- Android実装は後回し。
- ただし共通ゲームロジックをiOS専用APIに依存させない。
- OS固有コードは分離する。
- 将来Androidへ移植しやすい構造を維持する。

---

# 5. データ管理

- プレイヤーデータにはversionを持たせる。
- 新しいフィールドが増えても古い保存データを読み込めるようにする。
- 欠損フィールドには安全な初期値を設定する。
- データ破損時にゲーム本体まで起動不能にしない。
- ローカルキャッシュはサーバー上の正データを上書きする権限を持たない。

---

# 6. Phase 1 の実装範囲

現在の最優先Phaseは「プレイヤーデータ基盤」。

実装する：
- プレイヤー識別
- データモデル
- 読み込み
- 保存
- 復元
- データバージョン
- 将来サーバー管理へ拡張できる境界
- 必要最低限のエラーハンドリング

まだ実装しない：
- ショップ
- 実際のスキン購入
- デイリーミッション画面
- チャレンジ画面
- ランダムマッチ
- ランクUI
- 切断ペナルティUI
- 広告
- 課金
- Android固有対応

---

# 7. テスト

変更後は最低限以下を確認する。

- JavaScript構文エラーがない
- 起動できる
- 通常対戦が開始できる
- CPU対戦が開始できる
- オンライン部屋対戦の既存処理を壊していない
- 既存設定が保持される
- 新規プレイヤーデータが作成できる
- 保存後に再読み込みできる
- 古いデータまたはフィールド欠損時に安全に初期値補完できる

可能なら変更した機能に対して自動テストも追加する。

---

# 8. 作業報告

実装後は以下を簡潔に報告する。

1. 変更したファイル
2. 追加した機能
3. 既存機能への影響
4. 実行したテスト
5. 残っている注意点

大量の説明より、ユーザーが次に何を確認すればよいかを明確にする。

---

# 9. 判断に迷った場合

仕様が曖昧な場合、ゲームルールや既存挙動を推測で変更しない。
既存挙動を維持する安全側を選び、必要なら確認を求める。

`GAME_SPEC.md` を仕様の基準として扱う。
