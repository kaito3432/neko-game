# ランダムマッチ ランク・シーズン仕様

## 権威と対象

`OnlinePlayers` Durable Object がランダムマッチの正式結果を受け取り、サーバープロフィールへRP、戦績、通常勝利コインを保存する。クライアントから任意の勝敗・RP・コインを指定するAPIは設けない。`GameRoom` が生成した `matchId`、`winner`、`finishReason`、`completedAt` を使用する。

対象はplaying開始後の通常終了、`disconnectForfeit`、`turnTimeout`。部屋対戦、CPU、ローカル対人、preGame中止、`serverInvalid`、cancelled、勝敗未確定は対象外。同じmatchIdとplayerIdの結果はDurable Objectのトランザクション内で一度だけ反映する。

## RP・戦績・コイン

- 勝利：+10 RP、+5サーバー管理にゃんコイン、勝利数+1
- 敗北：-6 RP（最低0）、敗北数+1
- ランク：ブロンズ0、シルバー100、ゴールド250、プラチナ450、ダイヤ700、マスター1000以上
- ランク名は保存値を信用せずRPから再計算する。

既存のローカル `nyanCoins` は壊さず、今回のオンライン報酬は `serverNyanCoins` として分離する。表示時は取得済みのサーバー残高とローカル残高を合算する。オンライン報酬の確定値はサーバー側のみが更新できる。

## プロフィールフレーム

初回プロフィール補完でブロンズフレームを解放する。ランク到達時に、そのランク以下で未所持のフレームを `ownedProfileFrames` へ追加する。一度解放したフレームは降格やシーズン切替でも削除しない。`equippedProfileFrameId` の更新APIは所持済みIDまたは `default` だけを受け付ける。画像素材未用意のため、現段階は装飾付きCSSプレビューで表示する。

## 月次シーズン

seasonIdはサーバー時刻を日本時間へ変換した `YYYY-MM`。月またぎの試合は開始日時ではなく、サーバーで正式な勝敗が確定した `completedAt` の月へ加算する。クライアント時計は参照しない。

月替わり時は前シーズン終了時点の `finalRank`、`finalRP`、勝敗、報酬資格を `seasonHistory` に固定保存する。新シーズンRPは最終ランクごとにブロンズ0、シルバー50、ゴールド150、プラチナ300、ダイヤ500、マスター750。永久フレームは維持する。

## シーズン報酬

ブロンズはなし。シルバー200、ゴールド400、プラチナ800、ダイヤ1200にゃんコイン。次回オンライン画面表示時にモーダルを表示し、明示受取後に `seasonRewardsClaimed` へseasonIdを記録する。同一seasonIdの再送では二重付与しない。

マスター報酬は環境変数 `MASTER_REWARD_PERIODS` のJSONで3か月期単位に設定する。例：`{"2026-Q3":"正式なスキンID"}`。スキンIDはサーバーの既知報酬スキン表にも登録済みである必要があり、未登録素材を通常カタログへ仮公開しない。

設定がない期間は `rewardStatus: "pendingConfiguration"` とし、seasonId、finalRank、finalRPを含む資格を維持する。勝手にコインへ変換しない。後日正式スキンを設定すると、固定済み資格を元に受取可能へ変わる。設定済みスキンを本人が既に所持する場合だけ1500にゃんコインへ確定する。資格確定後のランク変化は報酬に影響しない。

## 互換性

既存プロフィールはアクセス時にversion 2へ補完し、RP 0、ブロンズ、当月seasonId、戦績0、serverNyanCoins 0、ブロンズフレーム所持から開始する。既存所持スキン、装備、CPU解放同期、切断履歴は保持する。
